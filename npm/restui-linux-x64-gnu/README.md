# restui-linux-x64-gnu

Prebuilt `restui` binary for Linux x64 (glibc). Installed automatically by the
[`restui`](https://www.npmjs.com/package/@will8ug/restui) package — do not depend on this directly.
